---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: complexgateds.
spec:
  group: ""
  names:
    kind: ComplexGated
    listKind: ComplexGatedList
    plural: complexgateds
    singular: complexgated
  scope: Namespace
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: ComplexGated is generated when (alpha AND beta) OR gamma is enabled
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComplexGatedSpec defines a CRD with complex precedence
            properties:
              complexField:
                type: string
            required:
            - complexField
            type: object
          status:
            description: ComplexGatedStatus defines the observed state of ComplexGated
            properties:
              complexReady:
                type: boolean
            required:
            - complexReady
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
