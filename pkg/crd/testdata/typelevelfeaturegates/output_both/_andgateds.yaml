---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: andgateds.
spec:
  group: ""
  names:
    kind: AndGated
    listKind: AndGatedList
    plural: andgateds
    singular: andgated
  scope: Namespace
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: AndGated is generated when both alpha AND beta feature gates
          are enabled
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AndGatedSpec defines a CRD that's generated when both alpha
              AND beta are enabled
            properties:
              andField:
                type: string
            required:
            - andField
            type: object
          status:
            description: AndGatedStatus defines the observed state of AndGated
            properties:
              andReady:
                type: boolean
            required:
            - andReady
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
