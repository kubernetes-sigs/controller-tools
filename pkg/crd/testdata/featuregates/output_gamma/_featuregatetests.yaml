---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: featuregatetests.
spec:
  group: ""
  names:
    kind: FeatureGateTest
    listKind: FeatureGateTestList
    plural: featuregatetests
    singular: featuregatetest
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: FeatureGateTest is the Schema for testing feature gates
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FeatureGateTestSpec defines the desired state with feature-gated
              fields
            properties:
              complexOrFeature:
                description: Complex precedence field - included when (alpha AND beta)
                  OR gamma is enabled
                type: string
              name:
                description: Standard field - always included
                type: string
            required:
            - name
            type: object
          status:
            description: FeatureGateTestStatus defines the observed state with feature-gated
              fields
            properties:
              complexOrStatus:
                description: Complex precedence status field - included when (alpha
                  AND beta) OR gamma is enabled
                type: string
              ready:
                description: Standard status field
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
